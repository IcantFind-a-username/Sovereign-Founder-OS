# S1-04 — 固定reporting检索与有序教学建议

**Revision 1 · 主线程独立审阅接受，Frozen；依赖未完成前不可分派。**Luna worker，主线程独立验收。
依赖S1-03实际接受、本 [guidance契约](../playground-guidance-contract.md)接受及
其中建议的S1-G04先行窄门接受；当前只准备设计，不声称任何前置运行通过。

## 精确写集与交付

仅 `crates/consultant-playground/src/domain.rs`、
`crates/consultant-playground/src/catalog.rs`。domain追加冻结DTO/纯方法及inline tests，
既有graph/动作/read_model不改；统一CATALOG追加契约5键，长度32，既有27键不动。
现catalog内容/结构测试更新为32项并保留前27项全部预期，不再建立另一份运行词典。
不改lib/模块闭包、gate/expected/fixture、manifest/lock、HTTP/UI、配置、规范或队列。
不是独占repo，不覆盖其他人改动；当前G03三文件不属本卡。

只读方法从session内部价格/stage产生输出，不接受读模型或任意参数。搜索只给两条
固定semantic hits；guidance价格优先。最终展示沟通建议和“仅示例两项修改完成”总结，
不新增call-done状态/第五动作，不声称真实沟通已完成；不安排或执行任何客户操作。

## RED→GREEN与命名验收

复用既有reachable_states和serde_json，模块头imports；测试预期来自契约表，不让
被测方法生成自身预期。新增domain测试：

- `teaching_guidance_matches_all_four_reachable_states`：四行精确核guidance全部字段/
  null/现有action ID，尤其250000+Customer仍先修价；最后无动作且总结仅示例完成。
- `reporting_search_is_fixed_and_teaching_reads_preserve_session`：四状态两hits顺序/
  query key恒定；重复读取前后整个session相等，search动作后输出不变，Reset输出回初始。
- `teaching_read_model_serializes_exact_shape`：to_value结构核外层5键、search两键/
  两hits各两键、guidance四键，固定成熟度值、正确字符串/布尔/数组/null类型。
- `teaching_output_keys_resolve_once_in_unified_catalog`：序列化统一CATALOG取得key集，
  全部输出*_key（跳过null）恰存在一次；suggested_action核现有两动作，不混作文案key。

catalog既有精确表/序列化/占位符测试扩充5行，确保32键唯一且双语全部对应本契约。
保留动作/DTO/物理边界所有回归。非法输入/反向转换/隐藏状态/代码注入由G04实际
变异测试验证；门失败回报主线程，不自行改expected或放宽grammar。

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

不提交/合并/推送，无额外报告文件；交付主线程base、diff --stat、新文件、真实RED/
GREEN/门、复用/新增工具及未完成项后停止。不宣称已有浏览器/网络/完整Playground。
复用RustLexer/boundary/source closure、domain/read_model/semantic keys/serde、统一catalog、
test_changed；新工具无，产品授权/模型/IO工具不调用。
复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
