# S2-06 — Complete today navigation and follow-up

**Revision 2 · Design candidate; Spec Frozen only when S2-00 is independently accepted.**
Worker implementation; dependencies: S2-05. Queue/claim status lives only in
[backlog](../../../backlog.md). No dispatch before dependency acceptance.

## Behavior and normative interfaces

Implement [business-demo-contract](../business-demo-contract.md) §9–13 final projections, navigation and checklist.
That file owns all fields, signatures, routes, invariants and failure semantics.
This card assigns the following bounded increment:

Add Followup inputs/FollowupCommand/apply_followup and three commands;
complete Summary priority/count/totals, related-object links and history view.
Put follow-up controls on Today and case details rather than a disconnected
app. Exercise-day advancement is explicit; never confuse real date with demo day.
All existing company/case/proposal/review/delivery/billing pages now form one
continuous path. Add the full browser founder checklist results to this card's
report (canonical steps remain contract§13). Final HTTP test drives the actual
release-compatible command through success/rework/reset and two fake roots.
Reuse FakeRoot and ChildServer; add no capture or measurement framework.

## Exact exclusive write set

- `apps/cli/src/business_demo.rs`
- `apps/cli/src/business_demo/followup.rs`
- `apps/cli/business-demo/app.js`
- `apps/cli/tests/business_demo_http.rs`

No implicit registration writes elsewhere. Existing listed files may be changed
only for this increment; new feature modules include their own unit tests.

## Required tests and browser evidence

Inline followup.rs: `business_demo_followup_completion_and_day`,
`business_demo_summary_priority_and_amounts`,
`business_demo_followup_references_and_limits`.
`business_demo_summary_priority_and_amounts` is table-driven over every named
row in contract§9, asserting exact next_step. The UI must link to that row's
destination; reviewer browser checks include draft, expiry and acceptance links. Required explicit
cases: stale_proposal, expired_draft, expired_submitted, expired_accepted_no_plan,
no_proposal, draft_to_submit, submitted_to_review, returned_or_rejected (both
outcomes), accepted_to_plan, open_delivery_task, completed_tasks_to_acceptance,
acceptance_to_invoice, draft_to_issue, outstanding_receivable, due_followup,
case_fallback and mutation_limit_advice. Include mixed-state fixtures to assert
first-match priority; expired submitted has pending_reviews0 and never advises
review, expired accepted without plan never advises create_engagement. All-done
planned milestones must advise acceptance draft before invoicing. The row's
command must satisfy its transition preconditions once valid form values are
provided; advice cannot bypass its backend checks. No planner framework.
HTTP names: `business_demo_complete_flow_and_rework`,
`business_demo_fake_roots_unchanged`, `business_demo_reset_invalidates_old_tab`.
Full-flow test asserts linked IDs/versions/snapshots and financial distinction,
with denial cases. Fake roots must have different canaries and unchanged full
snapshots after each process; no claim that random epochs are byte-identical.
Inherited launcher cleanup/output checks still run. Compare forbidden legacy
routes and review new import/call paths with existing lexer, no new scanner.

Reviewer browser check: execute every contract§13 step in a real browser, including failed server,
reset/restart and stale second tab. Capture screenshots at375x812 and1280x900;
roles remain Not connected, legal coverage Not reviewed. Do not report duration
as a measured outcome or user-observation result.

## Execution and acceptance

One card, one serial writer; preserve other changes. Implement only the linked
contract sections and this exact write set. No self-edits to cards, contract,
backlog, S1/legacy source, dependencies, gates or controller records. No commits,
merge, push, external messages or provider calls by the worker. Controller
records base/card/input blobs and handles reviewed local integration.

Worker: Luna medium, attempt budget30min, maxLunaFailures2; reviewer/fallback:
Astra high. Two substantive failures follow [protocol](../protocol.md) without
resetting attempt history. No speculative framework or automatic next card.

Run the exact per-card command block in [contract §12](../business-demo-contract.md#12-test-ownership-and-commands).
Enumerate and run this card's tests; zero/skipped mandatory tests are failures.
The reviewer runs the applicable full/S1 integration gates once for the wave,
checks actual browser behavior for this increment and binds results to the
candidate. No timers/usability metrics without an already-committed script.

Sole extra write: `docs/handoff/codex/reports/S2-06.md`. Raw evidence only in
`.harness/s2-06/`. Report base/revision/attempt/candidate binding, actual
visible behavior, diff stat, every new/untracked file, test names/counts and
commands with skipped/failure distinctions, reuse/new tools, deviations and
remaining work. Return candidate for independent review; do not self-accept.

Tool inventory: existing workspace::parse_amount_cents (only pure Workspace
call), serde/JSON, tiny_http, rand/hex; existing ChildServer::start_command,
stop/captured_output and raw_request/request HTTP helpers; FakeRoot, RustLexer,
S1 source/manifest/isolation tests, test_changed. Store/ops/render_document/
WorkflowRunner/authority/model/vault are not simulation APIs: their persistent,
UUID/time or authority effects remain outside this command. No Python benchmark
helpers exist here. Additional domain/form code is the only declared new tool;
no duplicate parser/capture/writer/hash/state framework.

> 复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
