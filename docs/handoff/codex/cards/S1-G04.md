# S1-G04 — Fixed search and guidance boundary

Revision 1 · Frozen. Luna medium; depends on accepted S1-03. Interface and
approved grammar are maintained only in
[the guidance contract](../playground-guidance-contract.md), revision2.
Read its S1-G04 section and fixed catalog increment before editing.

## Exclusive write set

- crates/consultant-playground/tests/support/boundary.rs
- crates/consultant-playground/tests/physical_boundary_teaching.rs (new)
- crates/consultant-playground/tests/support/fixtures/teaching-domain-additions.rs.txt (new)
- crates/consultant-playground/tests/support/fixtures/catalog-guidance-production.rs.txt (new)

Do not modify production, old fixtures, lexer, manifest, lock, docs or queue.
Do not grow the existing 1010-line physical_boundary_source.rs; retain its
source-closure and regression tests unchanged. You are not alone in the repo;
do not revert others' edits. No commits, merges or pushes.

## Required increment

The new domain alternative is the fixed read-model fixture followed by the
complete approved teaching code block. All older domain shapes remain accepted.
The new catalog alternative has exactly 32 entries: the original 27 plus the
five approved rows. Keep the original 27-entry alternative. Code, keys, order,
length and derives are fixed; only 64 en/zh ordinary-string positions in the
new fixture may vary, following the already accepted G03 algorithm. Reuse that
comparison; do not copy its mask logic or add a parser. No new source/lib shape.

Hand-build fixtures from approved contracts, never from candidate production.
Use include_str constants in boundary.rs. The new test file includes existing
RustLexer/boundary support; keep imports at module level. Existing exact wrapper,
path checks and error kinds remain unchanged. Avoid unused support imports.

Implement all three named tests and every mutation category from the contract.
Positive cases include old domain/catalog and both new complete fixtures; both
language data slots can vary but keys/code cannot. Negative cases must change
the intended accepted fixture, assert the relevant error category and cover
parameters, mutable receiver, external DTO input, graph writes, guidance
priority, fabricated call completion/fifth action, search hit/count/query,
Deserialize/reverse conversion, IO/env/process/unsafe/macros/functions; and
catalog missing/renamed/reordered/extra keys, length and executable expressions.
New fixtures retain exactly one terminal test module only in test wrappers.

First run the new accepted-fixture tests against the current boundary and keep
actual failure output. Then enable the narrow alternatives and run GREEN.
Do not overwrite RED with a later passing log or substitute a lock/build error.

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

Logs may use .harness/s1-g04/. Report base, diff --stat, new files, true RED/GREEN,
requirements covered, reused/new helpers and unfinished items; stop for review.
No browser, model, IO or product-state capability is delivered by this gate card.

Tool inventory: RustLexer/source_boundary, current catalog shape comparator,
exact terminal wrapper checker, fixed DTO/catalog fixtures, test_changed.
Reuse these; do not reimplement equivalent tools. Declare new helpers first.
