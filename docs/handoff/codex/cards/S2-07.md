# S2-07 — Independently accept the continuous synthetic business exercise

**Revision 1 · Design candidate; Spec Frozen only when S2-00 is independently accepted.**
Reviewer-only milestone. Dependencies: accepted S2-01A1, S2-01A2, S2-01B,
S2-02, S2-03, S2-04, S2-05 and S2-06. No product implementation attempt.

## Exact write set and role

- `docs/handoff/codex/reports/S2-07-review.md`

Raw evidence `.harness/s2-07/` only. Independent Astra high reviewer; controller
supplies exact stopped candidate and prior accepted evidence. No code/gate/card/
backlog changes, no commits, merge, push, provider calls or external actions.
Preserve others' work. Fixes go back to a bounded implementation card.

## Required acceptance

[Business contract](../business-demo-contract.md) §12 owns exact applicable
commands; §13 owns the founder checklist. Read actual code and accepted evidence.

1. Run/reuse bound exact-source unit/HTTP/Node/TypeScript evidence; enumerate all
   required names in implementation cards, no absent/zero/skipped pass. Run the
   exact full/S1 retained command block once for this candidate, reusing only
   valid unchanged-source evidence. Inspect size/new-file and duplication checks.
2. Inspect direct CLI dispatch, exact separate routes, approved dependencies,
   memory-only state, no application store/key/effect/model calls, and no S1 or
   legacy edits. Check declared tiny_http limits without importing stronger S1
   parser/deadline claims. Re-run S1 source/manifest hostile and release
   playground_isolation gates. Inspect actual two-fake-root S2 canary evidence.
3. Reproduce contract§13 in a real browser at375x812 and1280x900: editable
   company/case, evidence/assumptions, versioned quotes, return/revise/review,
   delivery tasks/results, invoice/payment distinction, followup/today, stale
   actions, process loss/restart and reset. Inspect labels, keyboard/focus,
   safe rendering and only literal same-origin network calls. Node/API results
   cannot replace this observation. Retain actual screenshots/console/network.
4. Compare legacy live routes using the exact S1-11 card procedure at the accepted
   pre-S2 baseline `ac4e09d759c5f1beebd8e3837d06ddd50d98fb59` and this candidate.
   Read [S1-11](S1-11.md) Live legacy route comparison: same synthetic copied
   roots/port and its narrow Date/exported_at_unix exceptions only; no other
   normalization. Keep S1 isolation's exact comparison rules unchanged.
5. Validate every financial/identity/reference invariant and immutable history;
   simulations must never emit owner approval, client acceptance, actual revenue,
   protected-data/Local Only claims, real model output or legal clearance.
6. Supply a current release build/launch command, exact commit/binary hash,
   usable local preview and the founder checklist. Confirm preview URL responds
   before saying it is live; distinguish stopped reviewer servers. S2 acceptance
   is progress, not full MVP acceptance or actual consultant usability.

Report accepted/changes_requested/blocked, exact candidate/card/input bindings,
actual checks and counts, artifact hashes, screenshots, observed platform,
legacy comparison, limits and missing observations. No timing percentages or
five-consultant claims without committed measurement and actual observations.

On acceptance nominate S3-00 design, explicitly retaining S3-M actual authority/
model egress and S3-L retrieval/legal dependencies and final MVP-00/01/02
integration. Do not dispatch unfrozen implementation, make a real model call,
or mark the continuing full goal complete.

Tools: existing release CLI, ChildServer/complete capture/raw HTTP, FakeRoot,
RustLexer/source/manifest gates, S1-11 legacy procedure, Node and TypeScript,
test_changed and preferred real-browser skill. No new framework or scanner.

> 复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
