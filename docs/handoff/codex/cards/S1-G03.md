# S1-G03 — Static bilingual catalog boundary

Revision 1 · Frozen by controller. Luna medium worker; depends on accepted S1-02.
Exact interface, approved 27-key table and grammar rules are maintained only in
[the catalog contract](../playground-catalog-contract.md), section S1-G03.
This card activates that reviewed proposal; it does not authorize product code.

## Exclusive write set

- crates/consultant-playground/tests/support/boundary.rs
- crates/consultant-playground/tests/physical_boundary_source.rs
- crates/consultant-playground/tests/support/fixtures/catalog-production.rs.txt (new)

No other files, production catalog/lib/domain, manifest, lock, lexer, docs or
queue changes. Other contributors may work in this repository; do not revert
their changes. Do not commit, merge or push.

## Implementation and evidence

Build the complete static catalog fixture from the approved contract, never
sample candidate production. Keep existing domain grammars and all old tests.
Reuse source_boundary/RustLexer and extract or parameterize the existing exact
terminal test-wrapper check; preserve domain error kinds. Catalog gets its
specific production and wrapper error kinds. Compare all tokens exactly except
54 en/zh ordinary string literal positions identified from the fixed fixture.
Key strings and code structure are not variable. No parser duplication.

The source-closure check must pair two files with the old lib grammar and three
files with the new catalog lib grammar. Reuse production_sources and source-root
checks. Test both directions of mismatches, unknown source files and symlinks.
A helper needed to share this check between real inventory and adversarial
fixtures stays in the existing three-file write set; no new generic framework.

Implement the three named acceptance tests from the contract. Each negative
mutation must actually differ from its accepted fixture and assert the intended
error category; checking only that an unrelated parse failed is insufficient.
Cover every listed structure/code/literal mutation, including byte/raw/C strings,
macros and trailing tokens. Test plain quoted en/zh data changes accepted while
all 27 keys remain fixed. Run RED with new accepted fixture before enabling it,
then GREEN with the implementation and all prior boundary regressions.

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

Report base, diff --stat, new files, actual RED/GREEN and checks, reused/new tools,
and any unfinished requirement, then stop for independent review. Logs may use
.harness/s1-g03/. Do not claim S1-03 or browser functionality complete.

Tool inventory: tests/support/RustLexer, source_boundary/strip_exact_test_module,
production_sources/source_root, existing source/symlink fixtures, test_changed.
Reuse these; do not reimplement equivalent tools. Declare any new helper first.
