# S1-11 — Independently review the runnable synthetic Playground

**Revision 2 · Independently reviewed and Frozen by controller. Dispatch only after dependencies are accepted.**
Reviewer-only card, dependent on all S1 feature/gate acceptances, including
S1-07 → S1-09 → S1-08 → S1-10A → S1-10. Bind a stopped candidate commit and exact
accepted evidence. The reviewer must be independent of its implementation.
This is an S1 checkpoint, not completion of the
[full MVP goal](../models-and-goals.md#131-当前推荐-goal交付全流程可演示-mvp).

## Exclusive write set

- `docs/handoff/codex/reports/S1-11-review.md` (new): independent verdict,
  bound evidence, limitations, preview instructions and next-stage handoff.

No product/test/gate/fixture/dependency changes, backlog/index/status changes,
README/roadmap maturity edits or new normative design. Raw review artifacts
may go only under `.harness/s1-11/`. Disposable baseline build directories and
synthetic roots are test artifacts, never user state. You are not alone;
preserve others' work. No commit/merge/push. Return changes_requested or
blocked with concrete findings if fixes or missing evidence are required;
the reviewer never repairs the candidate in place.

## Required evidence and independent checks

Read standalone-v2, accepted domain/action/catalog/guidance/HTTP/assets/server/
launch contracts, RFC 0004/0005 constraints and models-and-goals §13.1. Read
actual code and acceptance reports, not only their summaries. Record each
relevant implementation/gate commit and bind review to candidate HEAD.

1. **Physical scope and authority:** exact unpublished leaf dependency/source
   closure, only six embedded assets/eight routes/four closed actions, private
   non-serializable graph and one-way DTO. Re-run hostile source/manifest
   gates; independently inspect transport-only Instant/deadlines/startup IO
   exceptions. No Workspace/Vault/model/owner/effect capability enters the
   leaf; no runtime root/input/storage/config selector, remote resource or
   authority surrogate is introduced by CLI or browser.
2. **Real HTTP:** inspect S1-07 raw socket results and source bounds, Host/
   Origin/raw-target/duplicate/framing precedence, header/body limits, read/
   write deadlines, one request per connection, disconnect behavior, typed
   errors and nonmutation. Tests must have actually executed. A matching
   fixture alone is not proof of runtime correctness.
3. **Browser:** inspect actual S1-08 screenshots and network/observation
   artifacts at 375×812 and 1280×900 in both locales. Independently reproduce
   the four-action/guidance/search/Reset path and one failure/reload path using
   the real accepted CLI and preferred ego-browser skill. Confirm concurrent
   clicks/current-locale, stale-state hiding, keyboard/focus/targets/contrast/
   reduced-motion evidence exists; Node tests do not replace it. Do not infer
   screen-reader or non-specialist usability results from automated checks.
4. **Two-root isolation:** inspect accepted S1-10A capture/cleanup evidence
   and rerun S1-10 against the release binary.
   Verify different fixture roots/process PIDs, exact same-port complete
   transcript/stdout/stderr equality, no canary and unchanged inventories.
   Distinguish process nonmutation evidence from source-level absence of read
   capability. No normalization or test-child substitution is accepted.
5. **Existing product compatibility:** unchanged protected source/asset diff
   and S1-09's Ui help/Clap checks are necessary but insufficient. Produce or
   inspect actual baseline-versus-candidate live route transcripts as below,
   plus existing Workspace/export/disclosure/integrity/offline-verifier tests.
6. **Maturity:** visible copy and handoff describe a fixed synthetic,
   process-local exercise only. No claim of model use, contacted customer,
   persistence, recovery, owner authorization, protected real data or absolute
   security. RFC prerequisites are construction requirements for future work,
   not checks replaced by flags/fixture grants.

## Live legacy route comparison

Use the recorded pre-CLI S1-09 baseline binary or build that exact known commit
in an isolated disposable directory; never switch or edit the shared checkout.
Record baseline/candidate commit and binary hashes. Start each real command
`sovereign ui --port <same_unused_port> --no-open` against its own byte-identical
copy of one test-created legacy seed. Establish the seed once using baseline
CLI behavior with only synthetic values, then stop and copy it. This avoids
independent random identity generation being mistaken for a regression.
Use child-only fake platform paths, never the user's real app directory.

Capture stdout/stderr/exit and raw HTTP for GET `/`, `/assets/styles.css`,
`/assets/i18n.js`, `/assets/app.js`, `/favicon.svg`, `/api/state`,
`/api/command-center`, `/api/workspace`, `/api/export`;
POST `/api/verify-export` with the same captured valid seed export and with a
missing-bundle body; and an unknown route. `/api/state` must include disclosure
and integrity fields. Also capture `integrity` and `verify-export <same-file>`
CLI behavior. Exercise existing nonempty disclosure/export fixtures through
their established tests; do not silently characterize empty-list live checks
as nonempty disclosure coverage. Do not add new legacy routes.

Retain complete raw transcripts. The legacy tiny_http transport adds Date and
the existing export body has `exported_at_unix`; compare status, all other
headers and payload fields exactly, and independently validate these two
clock-derived values as well-formed/current within each recorded run window.
Any resulting Content-Length difference must be explained solely by the
export timestamp length. No other field/byte normalization is authorized.
Use standard HTTP/JSON tools; record exact comparisons, exceptions and input
seed. This narrow legacy comparator does not relax S1-10's absolute Playground
byte comparison. Any other baseline difference requires review, not masking.

## Final gate and report

On the bound candidate use one recorded complete final gate. Reuse a gate
already run on this exact unchanged candidate where appropriate; rerun only
missing commands or checks justified by a new finding. New frontend's accepted
tsconfig is at the crate root, superseding the old plan's assets-path example.

```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --locked
cargo build -p sovereign-cli --release --locked
cargo test -p sovereign-cli --release --locked --test playground_isolation -- --nocapture
node --experimental-default-type=module --test --test-reporter=tap crates/consultant-playground/tests/ui.test.mjs
npx -y -p typescript@5.5.4 tsc -p crates/consultant-playground/tsconfig.json
npx -y -p typescript@5.5.4 tsc -p apps/cli/assets/tsconfig.json
cargo tree -p sovereign-consultant-playground --locked
./scripts/check-file-size.sh
git diff --check
./scripts/test_changed.sh
```

Report `accepted`, `changes_requested` or `blocked`; candidate commit, reviewed
file/commit scope, each evidence path/hash and executed test counts, actual
browser/platform coverage, live legacy comparison, bound root-isolation proof,
remaining risks and observations not performed. Missing required checks cannot
be hidden behind a green aggregate or a zero/skipped test count. On acceptance
state precisely that S1's fixed Playground is runnable and observed under the
recorded conditions, not that real-user usability or the full MVP is proven.

Include the [launch contract](../playground-launch-contract.md) instructions:
`cargo run -p sovereign-cli -- playground --port 7788`, manual opening of the
printed URL, release binary `./target/release/sovereign`, four-action exercise,
language/Reset/reload/restart semantics and Ctrl-C stop. Do not require a
developer test executable to preview the product.

Keep five-consultant usability work **Target / not performed** unless actual
participant observations exist. The report may give the bounded future
protocol: five independent consultants try the fixed thread, switch language,
find guidance/search and Reset; record completion, misunderstandings and
observed obstacles with consent. Do not invent participants, timings, success
rates or professional validation. No user recruitment or messaging is
authorized by this card.

Nominate **S2-00 design** as the next work item after S1 acceptance. Its architect
must freeze the complete synthetic business domain/state graph, a separate
experimental surface and exact interfaces/write paths/tests while preserving
the S1 leaf and old UI boundaries. Proposal/review/delivery/billing/navigation
and later S3-M/S3-L integration remain Target until their own reviewed cards.
Do not dispatch S2 product behavior, relax a gate, add real model calls, or
declare the continuing full goal complete from this review.

Tool inventory: accepted CLI/run/startup URL; existing raw TCP/process/root
fixtures, Clap, RustLexer/source_closure/manifest and Node tests, preferred
ego-browser, standard HTTP/JSON tools and test_changed. No new implementation
or general review framework. Reuse these; do not reimplement equivalents.
Declare any new tool need before proceeding.
