# S1-G05 — Pure HTTP architectural boundary

Revision 1 · Frozen architectural task. Astra implements the gate and fixed
production fixture; a different strong reviewer accepts them. Ordinary S1-05
product implementation remains Luna medium. This routing follows protocol's
supervised architectural-boundary rule. Depends on accepted S1-04.

Read the complete [HTTP contract](../playground-http-contract.md); it alone
specifies the interface, trust boundary, ordered validation and responses.

## Exact write set

- crates/consultant-playground/tests/support/boundary.rs
- crates/consultant-playground/tests/physical_boundary_source.rs
- crates/consultant-playground/tests/physical_boundary_http.rs (new)
- crates/consultant-playground/tests/support/fixtures/http-production.rs.txt (new)

No production, old fixtures, lexer, manifest, lock, docs, assets or CLI changes.
Other contributors may work in this repo; do not revert their edits. Do not
commit, merge or push. Report any necessary scope change before proceeding.

## Required result

Write one complete production fixture implementing the already approved pure
handler contract, including all 11 errors and exact validation order. It is
architectural scaffold, not installed product code; do not sample a later
candidate to generate expected. Preserve all earlier source grammars.

Extend source_boundary for http.rs with its specific production/wrapper error
kinds, reusing the existing terminal-wrapper and path checks. The exact fixture
pins allowed Mutex/serde/domain/catalog use; it does not broadly allow std/IO.
No new JSON/HTTP parser: request JSON remains serde_json, framing stays outside.

Extend the existing shared source_closure_boundary in physical_boundary_source
rather than copying it. Retain old 2/3-file pairs; add exactly 4 files with the
new lib declaration. HTTP without catalog is not a stage. Exercise new valid
pair and both mismatch directions plus missing/unknown files and http symlink
using the same helper as the real inventory. Keep that file below 1200 lines;
put large grammar/mutation suites in new physical_boundary_http.rs. Reuse or
parameterize the existing fixture constructor; no second source scanner.

Implement the three named G05 tests and every required mutation in the HTTP
contract. Test exact new fixture, meaningful body/Host/Origin/enum/authority/
poison/CORS/input-reflection widening, and wrapper/module/path/symlink rejection.
Each mutation must target the relevant valid code and change it; assert specific
error kind, not an unrelated syntax failure. Prove new valid fixture fails before
the gate change, then passes afterward; preserve distinct RED/GREEN logs.

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

Logs .harness/s1-g05/. Report base, diff stat, new files, true RED/GREEN, checks,
reused/new helpers and remaining issues, then stop for independent review.
Tool inventory: RustLexer/source_boundary/wrapper/path, shared source closure,
ManifestFixture/source-root fixtures, serde/serde_json and test_changed.
Reuse these; do not reimplement equivalent tools. Declare new helpers first.
