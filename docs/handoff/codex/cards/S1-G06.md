# S1-G06 — Embedded assets architectural boundary

Revision 1 · Frozen architecture task. Astra implements the gate and small Rust
fixture; a different strong reviewer accepts. Depends on accepted S1-05.
Product assets/UI remain the Luna S1-06 task. Read the full
[assets contract](../playground-assets-contract.md) for exact mapping and behavior.

## Exclusive write set

- crates/consultant-playground/tests/support/boundary.rs
- crates/consultant-playground/tests/physical_boundary_source.rs (closure/lib wiring only)
- crates/consultant-playground/tests/physical_boundary_assets.rs (new)
- crates/consultant-playground/tests/support/source_closure.rs (new, pure extraction first)
- crates/consultant-playground/tests/support/fixtures/assets-production.rs.txt (new)

No production assets, old fixtures, lexer, manifest, lock, docs, CLI or unrelated
tests. Other contributors may work here; do not revert their changes. Do not
commit, merge or push. Report any needed scope change before editing outside it.

## Gate and fixture

Freeze only the small exhaustive Rust AssetRoute mapping and six literal
include_bytes targets, not a duplicate HTML/CSS/JS website. Reuse RustLexer,
source_boundary and exact wrapper/path checks; add the two asset-specific error
kinds. All previous source shapes remain. New exact five-file source closure
must pair with the new lib declaration; extend the existing shared inventory
helper, never copy the scanner. Verify the expected lib is rustfmt-stable.

Use source-root checks and a bounded six-file directory assertion to reject
missing assets, extra/nested files, root/file symlinks and path escapes. The old
stage can have no assets directory; an admitted assets module requires all six
ordinary files. Tests use temporary placeholder assets, not user files. Validate
fixture fmt and isolated compilation with the already accepted http/domain/
catalog files and six placeholders before handing the scaffold to Luna.

Implement all three named G06 tests and mapping/path/module/wrapper/symlink
mutations from the contract. Exercise the real inventory helper as well as
individual source validation. Record the complete new fixture/closure failing
against the old gate, then passing after the gate implementation. Retain RED
separately; malformed placeholders are not evidence of a valid new shape.

First extract the existing closure/error/fixture helpers into the owned
source_closure.rs, preserving behavior and confirming old tests pass. Then extend
that shared helper for the asset stage and use it from both test binaries.
Do not grow physical_boundary_source.rs past 1200 lines; put the new attack
matrix in physical_boundary_assets.rs. No copied scanner or parallel fixture implementation.
No broad include allowance, new JSON/JS parser, dependency or runtime filesystem
access. HTML/CSS/JS capability review, Node tests and actual browser network
checks remain S1-06/server acceptance obligations; passing this file/mapping
gate alone cannot be advertised as proving arbitrary JavaScript safe.

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

Logs .harness/s1-g06/. Report base, diff stat, new files, true RED/GREEN,
fixture compile/fmt, checks, reused/new helpers and unfinished requirements;
stop for independent review. Product/browser functionality is not yet delivered.
Tool inventory: RustLexer/source_boundary/shared wrapper/path, current shared
source inventory, source_root/ManifestFixture/symlink fixtures, test_changed.
Reuse these; do not reimplement equivalents. Declare new helpers first.
