# S2-01A2 — Add the editable customer opportunity and discovery

**Revision 3 · Spec Frozen: independently admitted S2-00 first-slice scope.**
Worker implementation; dependencies: S2-01A1. Queue/claim status lives only in
[backlog](../../../backlog.md). No dispatch before dependency acceptance. S2-00 partial design admission for this
card is recorded in [the controller report](../reports/S2-00-supervised.md);
later S2 design findings do not reopen this independently reviewed scope.

## Behavior and normative interfaces

Implement [business-demo-contract](../business-demo-contract.md) §3–4 case fields, §9 jurisdictions/availability, §11.
That file owns all fields, signatures, routes, invariants and failure semantics.
This card assigns the following bounded increment:

Move Company/Service/CompanyInput and save_company from root into case.rs
without changing accepted behavior. Add CaseInput/save_case and complete Case
except discovery.assertions, which S2-01B introduces. Server owns all six fixed
IDs. Root registers the module, SaveCase command/data, full case view and
role/legal availability. Roles all not_connected; legal all not_reviewed.

Add opportunity list, customer/case editor and shared navigation; no manually
copied fields. Company and case saves advance input_revision; reject invalid
budgets/text/region shapes atomically. Add editable region/transaction fields
and explicit unknown subdivision states. No arbitrary JSON, real contact address
or location inference. This is one practice case, not a general CRM.

Summary now includes currency, projection_cents=0, accepted_order_cents=0,
invoice_drafts_cents=0, simulated_invoiced_cents=0, simulated_received_cents=0,
simulated_outstanding_cents=0, open_clarifications=0, pending_reviews=0,
open_tasks=0, due_followups=0 and next_step=current opportunity.next_step.
Zero means no corresponding exercise records; UI labels later unavailable
feature sections as Later stage. Later cards replace those derived calculations.

## Exact exclusive write set

- `apps/cli/src/business_demo.rs`
- `apps/cli/src/business_demo/case.rs`
- `apps/cli/business-demo/app.js`
- `apps/cli/tests/business_demo_http.rs`

No implicit registration writes elsewhere. Existing listed files may be changed
only for this increment; new feature modules include their own unit tests.

## Required tests and browser evidence

Rust names in case.rs: `business_demo_case_links_and_edit`,
`business_demo_budget_validation_is_atomic`,
`business_demo_region_unknowns_are_preserved`,
`business_demo_customer_classification_grants_nothing`,
`business_demo_region_and_transaction_wire_roundtrip`.
The roundtrip table deserializes CaseInput then serializes the saved Case: pin
SG/EU/US/unknown, B2B/B2C/unknown and explicit null subdivisions; include EU/US
uppercase subdivision values. Assert emitted Coverage region codes too. Reject
lowercase sg/eu/us/b2b/b2c aliases without mutation. Check JSON keys/values
structurally, not substring matches; unknown/null must survive unchanged.
Add HTTP test `business_demo_case_roundtrip_and_no_legacy_routes` exercising
save_case, GET reload and unknown /api/workspace, /api/export and model paths.
Retain all A1 tests and the exact CLI/S1 dispatch behavior.

Reviewer browser check: change invented organization/contact/problem/budget; navigate all three
early pages and reload with state intact. Mark customer and observe no order,
review or income created. SG/EU/US coverage remains Not reviewed.

## Execution and acceptance

One card, one serial writer; preserve other changes. Implement only the linked
contract sections and this exact write set. No self-edits to cards, contract,
backlog, S1/legacy source, dependencies, gates or controller records. No commits,
merge, push, external messages or provider calls by the worker. Controller
records base/card/input blobs and handles reviewed local integration.

Worker: Luna medium, attempt budget30min, maxLunaFailures2; reviewer/fallback:
Astra high. Two substantive failures follow [protocol](../protocol.md) without
resetting attempt history. No speculative framework or automatic next card.

Run the exact per-card command block in [contract §12](../business-demo-contract.md#12-test-ownership-and-commands).
Enumerate and run this card's tests; zero/skipped mandatory tests are failures.
The reviewer runs the applicable full/S1 integration gates once for the wave,
checks actual browser behavior for this increment and binds results to the
candidate. No timers/usability metrics without an already-committed script.

Sole extra write: `docs/handoff/codex/reports/S2-01A2.md`. Raw evidence only in
`.harness/s2-01a2/`. Report base/revision/attempt/candidate binding, actual
visible behavior, diff stat, every new/untracked file, test names/counts and
commands with skipped/failure distinctions, reuse/new tools, deviations and
remaining work. Return candidate for independent review; do not self-accept.

Tool inventory: existing workspace::parse_amount_cents (only pure Workspace
call), serde/JSON, tiny_http, rand/hex; existing ChildServer::start_command,
stop/captured_output and raw_request/request HTTP helpers; FakeRoot, RustLexer,
S1 source/manifest/isolation tests, test_changed. Store/ops/render_document/
WorkflowRunner/authority/model/vault are not simulation APIs: their persistent,
UUID/time or authority effects remain outside this command. No Python benchmark
helpers exist here. Additional domain/form code is the only declared new tool;
no duplicate parser/capture/writer/hash/state framework.

> 复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
