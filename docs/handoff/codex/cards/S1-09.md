# S1-09 — Add the real CLI launch and preserve existing commands

**Revision 1 · Independently reviewed and Frozen by controller. Dispatch only
after the dependencies below are accepted.**
Depends on actual S1-07 acceptance and stopped writer, not S1-08. Controller
uses the accepted order in the [launch contract](../playground-launch-contract.md)
and binds the dispatch base/card blob.
Luna medium implements one stopped candidate; existing protocol attempt limits
and independent review apply. This card introduces the usable preview command.

## Exclusive write set

- `apps/cli/src/main.rs`: sole Playground variant/arm and test module declaration.
- `apps/cli/src/cli_tests.rs` (new): Clap parsing/help and compatibility assertions.
- `apps/cli/Cargo.toml`: sole normal workspace Playground dependency.
- `Cargo.lock`: sole CLI → Playground dependency edge.

No root manifest, existing Ui/help source, ui.rs, old assets, Workspace,
domain/HTTP/assets/server, gate/fixture, scripts, queue or normative-doc edits.
You are not alone; preserve other contributors' work. No commit/merge/push.
Logs may be written under `.harness/s1-09/`; controller owns the durable report
`docs/handoff/codex/reports/S1-09-supervised.md` after reviewing the candidate.

## Exact implementation and tests

Follow the launch contract's exact `Commands::Playground { port: u16 }`,
`#[arg(long, default_value_t = 7788)]`, and direct
`sovereign_consultant_playground::run(port)?` arm. Existing Ui variant/arm and
all other command bodies are unchanged. No open-browser/root/environment path.

Write these named tests first, preserving actual RED before implementation:

- `playground_clap_port_and_option_contract`: default 7788 and explicit 0/7788/
  65535 accepted; 65536, negative, nonnumeric, missing value, unknown flag,
  `--root`, `--no-open`, and extra positional rejected. Use a table of inputs
  and expected values/errors; no custom parser.
- `playground_help_lists_only_port_and_builtin_help`: top-level lists the new
  command; subcommand help is successful and exposes only port plus Clap help,
  with default 7788. Read Clap command metadata for option closure rather than
  a hand-written help parser.
- `ui_clap_and_dispatch_remain_compatible`: default/explicit port and no_open
  parsing unchanged; exact old Ui variant/arm pinned; Playground arm passes
  only port directly and does not call data_dir/open-browser code. Structural
  check is not proof of root isolation, which belongs to S1-10.
- `playground_release_launch_smoke`: actual compiled CLI process, startup URL
  authority, GET complete fixture, six real assets/MIME, one fixed action and
  resulting GET, Reset, occupied-port nonzero exit, bounded cleanup/reap.
  This may be a recorded terminal-tool acceptance case if root selects that
  path; identify it as manual process evidence, never as a Rust test count.

**Selected smoke method:** controller or worker uses recorded terminal process
and HTTP tool calls for the same smoke assertions, with bounded IO and explicit
process stop/exit evidence. No new integration-test file or adapter is permitted.
Report this as manual process evidence, never as a Rust test count. Reuse existing
transport tests rather than copying their raw parser/child helpers. Compile
failure is interface RED, not behavioral RED; preserve this distinction.

Before edits capture baseline Ui help from the real binary, then compare raw
candidate help bytes/status. Save both under `.harness/s1-09/`. Record unchanged
legacy source/assets diff and actual existing CLI test names/counts including
Workspace/export/disclosure/integrity/offline verification. Do not claim live
old-route or two-fake-root evidence from parser/static tests.

```bash
cargo test -p sovereign-cli --locked
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-cli --all-targets --locked -- -D warnings
cargo fmt --all --check
cargo build -p sovereign-cli --release --locked
cargo tree -p sovereign-consultant-playground --locked
./scripts/check-file-size.sh
git diff --check
./scripts/test_changed.sh
```

Cargo.lock changes already force test_changed's full scope; controller records
one integration gate, avoiding duplicate full runs without a new change.
Record release binary path/hash and exact launch/GET commands/results. The
controller's report includes base/diff stat/new files, original RED/GREEN,
helper reuse, legacy compatibility evidence, smoke lifecycle and preview
instructions from the launch contract. No skipped/zero named cases accepted.
Next card is actual browser S1-08; two-root isolation/final S1 review remain.

Tool inventory: existing Commands/main/Ui, Clap Parser/CommandFactory, public
leaf run and fixed stdout URL, accepted S1-07 transport helper, serde_json,
RustLexer/source_closure/manifest gate, existing CLI tests/test_changed.
No new production or test framework. Reuse these; do not reimplement equivalents. Declare new helpers first.
