# S1-02 — 可序列化单向读模型

**Revision 1 · Frozen。**Luna worker；依赖DTO设计及S1-G02实际接受。
唯一接口：[DTO契约](../playground-contract.md)。不依赖S0 harness。

精确写集：`crates/consultant-playground/src/domain.rs`、
`crates/consultant-playground/Cargo.toml`、`Cargo.lock`。按冻结grammar改四处可见性并
附加DTO，tests在原inline模块；manifest仅加二项workspace普通依赖，lock仅leaf两边。
不改根Cargo.toml、gate/expected/lib/配置/规范/队列，不升级依赖、不回退其他人改动。

先写RED再GREEN；复用reachable_states，预期来自固定表而非read_model自己生成：

- `read_model_serializes_exact_fields_in_every_reachable_state`：四状态to_value解析
  对象后核精确15键集合/类型/值（非子串），metadata固定，价格/阶段随动作，其余恒定。
- `read_model_reads_preserve_session_and_are_deterministic`：重复read_model/to_string
  输出一致，完整session不变，search前后输出相等。
- `read_model_snapshot_is_detached_and_reset_restores_projection`：旧DTO不随session
  动作变化；改本地DTO副本不改session；四状态Reset的JSON等于初始全部15字段。

保留旧8个unit及物理边界测试。无Deserialize/DTO→graph/Workspace、graph公开或
Serialize；反向/输入/副作用拒绝由G02精确grammar恶意fixtures实际证明。不能为过门
更改expected；错误报告主线程裁决。不增加IO、HTTP、JSON解析业务输入或自写serializer。

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

不提交/合并/推送，无额外报告；回报diff --stat/新文件/真实RED-GREEN/门/工具后停。
复用serde/serde_json、既有fixture与RustLexer/boundary/manifest/json/test_changed，
新工具无；不调用WorkflowRunner/AuthorityStore/AuditLedger/ModelProvider。不宣称UI已完成。
复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
