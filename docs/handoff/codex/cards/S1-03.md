# S1-03 — 可直接供浏览器使用的双语 catalog

**Revision 1 · 主线程独立审阅接受，Frozen；依赖未完成，尚不可分派。**主线程拆卡/独立验收，Luna施工。
依赖S1-02实际接受、本 [catalog契约](../playground-catalog-contract.md)接受，以及
其先行S1-G03窄门独立接受。S1-02的Frozen不表示已实现；不要求自动S0 harness。
G03建议只在契约中给出，主线程裁决/冻结后先执行；本卡不能自行修改gate。

## 精确写集与接口

- `crates/consultant-playground/src/catalog.rs`（新增，含inline tests）
- `crates/consultant-playground/src/lib.rs`（仅契约精确catalog模块声明）

完整27键、en/zh文案、CatalogEntry和CATALOG接口沿唯一契约；不另维护JS字典。
没有lookup框架、Locale存储、语言解析器、占位符引擎或额外运行模块；未来浏览器
直接消费serde序列化的同一数组。名字/邮箱/钱与graph状态仍来自既有read_model。
不改domain/DTO、gate/fixture、manifest/lock、assets/HTTP/CLI、规范/队列或配置。
不是独占repo，不回退别人改动；当前G02 fallback的六测试文件不属于本卡。

## RED→GREEN与命名验收

先inline unit RED，再最小静态数组GREEN；测试冻结预期按契约表，不从CATALOG反向
生成。所有imports在模块头，重复输入表驱动/共享fixture，不新增test runtime依赖。

- `catalog_contains_exact_complete_bilingual_table`：27条固定顺序、每key唯一、en/zh
  非空、全部文案逐项等于契约；实际语义key及lead/customer每个恰出现一次。
- `catalog_serializes_exact_browser_shape`：serde_json::to_value后结构性核数组长27、
  每对象键恰为key/en/zh且均字符串，与契约表一致；不使用JSON子串断言。
- `catalog_has_matching_empty_placeholder_sets`：全部en/zh无花括号占位符，集合均空；
  不引入占位符解析器。后续需要占位符时先更新本契约。

CATALOG没有session参数或引用，不增加“读常量后状态不变”的重复测试；语言切换
不改graph/session的行为留到实际前端集成测试验证。

代码/宏/自由key/反向转换/未知module/路径逃逸由已接受G03变异测试实际验证。
必须保留既有动作/DTO/物理边界全部回归，不删测试或自行刷新expected来过门。

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

不提交/合并/推送，无额外报告文件；回报主线程base、diff --stat、新文件、真实
RED/GREEN/门、复用/新增工具与未完成项后停止。不宣称已有页面、检索或完整S1。
复用既有RustLexer/boundary/production_sources/source_root、ManifestFixture/JsonParser、
serde DTO与test_changed；新增工具无，产品授权/模型/IO工具不调用。
复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
