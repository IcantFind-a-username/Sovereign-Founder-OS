# S1-G01 — 动作 grammar 的窄门迁移

**Revision 1 · Frozen · Luna worker；依赖 S1-00 独立接受。**
主线程冻结 base/card blob 后分派，独立 reviewer 验收；不依赖自动 S0 harness。
唯一接口/grammar：[playground-contract](../playground-contract.md)；不改生产源。

## 精确写集与实现

- `crates/consultant-playground/tests/support/boundary.rs`
- `crates/consultant-playground/tests/physical_boundary_source.rs`
- `crates/consultant-playground/tests/support/fixtures/action-domain-additions.rs.txt`（新增）

无额外报告文件，回报主线程。其他源、gate、manifest、lexer、规范和队列禁止修改。
不独占 repo；保留其他人的改动，同一写集不得并发。
保留 `source_boundary(path: &Path, source: &str) -> Result<(), SourceBoundaryError>`
及全部错误种类；原 `EXPECTED_DOMAIN_PRODUCTION` 不改其字节。
fixture 恰为契约代码块，人工固定；boundary.rs 以相对 include_str! 读取，复用
RustLexer，比较原 token 或原 token 加 fixture token 两种完整序列。
为测试允许将原常量及 `ACTION_DOMAIN_ADDITIONS: &str` 设为 pub(crate)；不写新
parser。独立 fixture 源用两常量加精确测试 wrapper 构造，不读取候选 domain.rs
生成 expected。路径属性与测试 wrapper 先照旧验证。

## 命名验收与拒绝案例

先写实际 RED 再最小 GREEN，现有全部边界测试保留。新增：

- `action_gate_accepts_pinned_legacy_and_action_grammars`：两个独立静态 fixture
  均通过；当前尚无动作的真实生产代码通过。
- `action_gate_rejects_unapproved_action_mutations`：表驱动验证第五动作、unit
  变 tuple/string/price 参数、错误金额/stage、额外字段写入、搜索修改、Reset
  保留旧值/IO、附加方法/模块/导入/属性、cfg 隐藏生产代码均拒绝。
- `action_gate_preserves_escape_and_test_wrapper_rejections`：动作 fixture 上
  验证 grouped std/alias、env!、format!、allocation、unsafe/extern/asm、path
  属性、改/删/非终结 test wrapper 拒绝；沿用错误分类，不只 assert is_err。

旧文件闭包/source-root/symlink/manifest 回归照常执行；fixture 不属 src。
旧源码绿色、新 fixture 通过且恶意变异拒绝后独立审阅，才派 S1-01。

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

报告 base/diff --stat、新文件、RED/GREEN 结果和未运行项，交付即停；不提交/
合并/推送，主线程审阅后处理。新增工具无，只有受审语法 fixture。
工具：复用 tests/support/{boundary,rust_lexer,production_sources,source_root,
manifest,json,symlink_fixture} 和 scripts/test_changed.sh；不调用产品工具。
复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
