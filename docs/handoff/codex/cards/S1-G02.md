# S1-G02 — DTO source 与 manifest 的配对窄门迁移

**Revision 1 · Frozen。**Luna worker；依赖S1-01实际接受及
[DTO契约](../playground-contract.md)接受。主线程分派/验收，不依赖S0 harness。
本卡合并原G02依赖门和本阶段必要source门；旧生产不变，禁止提前开放未来能力。

精确写集（只此六文件）：

- `crates/consultant-playground/tests/support/boundary.rs`
- `crates/consultant-playground/tests/physical_boundary_source.rs`
- `crates/consultant-playground/tests/support/fixtures/read-model-domain.rs.txt`（新增）
- `crates/consultant-playground/tests/support/manifest.rs`
- `crates/consultant-playground/tests/support/json.rs`
- `crates/consultant-playground/tests/physical_boundary_manifest.rs`

完整新生产fixture人工由旧固定常量+动作、契约4处可见性替换和DTO块组成，不含test
wrapper；fixture不是从候选源码刷新。boundary以pub(crate)常量READ_MODEL_DOMAIN_PRODUCTION
和include_str读取，复用RustLexer精确比较第三形状；前两形状/常量保持。全部path/
wrapper/source closure/恶意代码检查保留，不能按名字/关键字放行任意方法体。
manifest_boundary签名不变，metadata只接受契约两种完整依赖集合，复用ManifestFixture。
为准确区分开关，现有JsonValue增加Bool(bool)/Null，parse_keyword分别返回真/假/null，
数字仍Scalar；不另建parser。新测试直接核三种值及错误类型，未知输入不能当null。
不改生产manifest/Cargo.lock/source/lib、原lexer或队列。

新增命名测试，实际RED→GREEN，旧测试不删除或放宽：

- `read_model_gate_accepts_only_pinned_stage_grammars`：三个独立静态fixture通过，
  动作缺失、只可见性扩展/只附DTO等不完整新形状拒绝，旧生产绿色。
- `read_model_gate_rejects_projection_mutations`：表驱动拒绝metadata/key/类型/字段/
  值来源改变，graph公开/序列化、DTO Deserialize/反向From/自定义serializer、&mut
  self/graph引用/额外参数/写入；保留IO/env/process/unsafe/path/wrapper变异回归。
- `manifest_accepts_only_empty_or_exact_dto_dependencies`：空及冻结完整二项通过。
- `manifest_rejects_dto_dependency_mutations`：逐项覆盖契约全部字段、单项/第三项、
  dev/build/target/dotted/alias/optional/features/default/source/path/version逃逸。
- `metadata_json_distinguishes_boolean_null_and_number`：true/false/null/数字分别识别，
  错误类型不满足依赖字段；publish/features/build/lib/source-root旧拒绝测试继续运行。

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

不独占repo，不回退别人改动；不提交/合并/推送，无额外报告文件。回报base/diff
--stat/新文件/RED-GREEN/门/复用与新增工具后停；G02独立接受才派S1-02。
复用RustLexer、boundary、production_sources/source_root、manifest/json/symlink fixtures
及test_changed；只扩现有JSON值表示，新通用框架无，产品工具不调用。
复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
