# S1-G07 — Loopback transport architectural boundary

**Revision 1 · Independently reviewed and Frozen by controller; dispatch only
after the dependencies below are accepted.**
Astra writes the gate/fixture; another strong reviewer accepts. Dependencies:
accepted [server contract Rev1](../playground-server-contract.md), actual
S1-06 acceptance, G06 shared source_closure extraction, and stopped prior
writers. This card does not implement or run the product server.

## Exclusive write set

- crates/consultant-playground/tests/support/boundary.rs
- crates/consultant-playground/tests/support/source_closure.rs
- crates/consultant-playground/tests/support/manifest.rs
- crates/consultant-playground/tests/physical_boundary_server.rs (new)
- crates/consultant-playground/tests/support/fixtures/server-production.rs.txt (new)

No physical_boundary_source.rs growth, old fixture edits, lexer/parser rewrite,
production source/assets/manifest/lock, CLI, docs or queue edits. You are not
alone; preserve other contributors' changes. No commit, merge or push.

## Exact change and tests

Keep existing source_boundary, source_closure_boundary, manifest_boundary and
validate_dependencies signatures. Add ServerProductionShape and
ServerTestModuleShape using existing RustLexer/wrapper/path helpers. Admit only
the complete fixture's production tokens plus the exact terminal inline test
module. Add the six-file source inventory and its paired rustfmt-stable lib
shape: old declarations + private server + `pub fn run(port: u16) ->
std::io::Result<()> { server::run(port) }`. Older shapes and rejections remain.

Write the server fixture from the accepted contract, including all listed
interfaces, literal bind, trusted actual port, bounded reads/deadlines, strict
httparse/framing policy, handler/assets integration, exact framing and no
drain/retry. Declare any needed additional private helper before implementing
it; final helper signatures belong in the reviewed fixture. No blanket std,
network/time, parser, include, module, lint or IO allowance. Reviewers must
inspect actual logic, not only token matching.

Extend the existing dependency validator exactly once to allow the pinned
httparse tuple only as part of serde/serde_json/httparse. manifest_boundary
pairs presence of server.rs and that dependency tuple in both directions;
zero/two-dependency historical fixtures remain accepted. Keep no path/rename/
optional/target/dev/build dependencies, features or alternate source targets.
Reuse ManifestFixture and the G06 six-placeholder asset fixture. No duplicate
scanner, metadata parser, directory walker, domain/catalog or website fixture.

Add these named tests with table-driven legal and hostile cases:

- `server_gate_accepts_old_and_pinned_next_closure`: all prior stages and full
  next stage pass; missing server/lib/assets or mismatched pairing rejects.
- `server_gate_rejects_transport_authority_and_resource_widening`: nonliteral/
  wildcard/IPv6 or second bind, outbound connect, Host-derived port, new public
  API/backend, body/head/header/deadline expansion, unbounded read/drain,
  per-read budget reset, permissive parser, TE/duplicate-CL acceptance, lost
  duplicate Host/Origin/Content-Type, arbitrary route/file, retry/reset/CORS/
  Date/clock-derived state and production test switch all reject.
- `server_gate_preserves_path_wrapper_and_asset_boundaries`: extra source,
  path/symlink/wrapper/tail-code attacks and old six-asset attacks remain red.
- `server_manifest_accepts_only_stage_paired_parser_dependency`: old/new exact
  declarations pass; missing/extra/parser-on-old-stage/wrong-version/defaults/
  features/rename/path/source/kind/target/optional declarations reject, via
  real Cargo metadata plus existing direct metadata mutation helpers.

Capture full valid next-stage fixture failing against the old gate, then
passing the new gate; no malformed placeholder RED. Compile/lint/rustfmt the
new fixture in an isolated temporary crate with accepted domain/catalog/http,
G06 asset fixture, six placeholder files and exact parser dependency. This
fixture compile may create only temporary manifests/locks, not workspace
production edits. Preserve source-boundary named error assertions.

```bash
cargo test -p sovereign-consultant-playground --locked
cargo clippy -p sovereign-consultant-playground --all-targets --locked -- -D warnings
git diff --check
./scripts/test_changed.sh
```

Logs .harness/s1-g07/. Report actual base/diff stat/new files, true RED/GREEN,
fixture compile/fmt results, named test counts, reused/new helpers and gaps;
stop for independent review. Controller runs workspace tests/clippy/fmt and
dedup/file-size scan once for the stopped candidate. No zero/skipped acceptance
tests. Python pytest/ruff/mypy commands do not apply to this Rust increment.

Tool inventory: RustLexer/source_boundary/shared wrapper/path;
G06 source_closure/source_root/six-placeholder assets; ManifestFixture/
manifest_boundary/validate_dependencies; accepted HTTP and assets fixtures;
test_changed. New tool is httparse from the accepted amendment, no equivalent
parser implementation. Reuse above; do not reimplement equivalents. Declare
additional helpers first.
