# S2-01A1 — Run the company and service experiment

**Revision 3 · Spec Frozen: independently admitted S2-00 first-slice scope.**
Worker implementation; dependencies: S2-00. Queue/claim status lives only in
[backlog](../../../backlog.md). No dispatch before dependency acceptance. S2-00 partial design admission for this
card is recorded in [the controller report](../reports/S2-00-supervised.md);
later S2 design findings do not reopen this independently reviewed scope.

## Behavior and normative interfaces

Implement [business-demo-contract](../business-demo-contract.md) §1–3, §4 company/service subset, §10–12.
That file owns all fields, signatures, routes, invariants and failure semantics.
This card assigns the following bounded increment:

Add the separate BusinessDemo Clap variant with u16 port default7789 and its
direct run arm. Implement the tiny_http loop, four routes, bounded JSON/header
validation, epoch/revision state, detached view and atomic save_company/reset.
Initial Case contains only id/input_revision/company/service. Other feature
fields are not emitted yet. Company/service structs and save_company initially
live in business_demo.rs; A2 moves them unchanged into case.rs.

Build an actual company/service form with save, inline reset confirmation,
linked Today summary and failure/reload controls. Company/service seed and field
rules are contract§4. There is no lead/quote simulation stub. available_commands
is exactly save_company/reset. initial summary contains currency and next_step
only (next_step="Enter the practice customer and discovery in the next increment").
Roles/legal and other summary fields are introduced by A2/later cards.

One app.js module exports requestFor/formatMoney/render/refresh/dispatch;
proposalDiff is not introduced until S2-02. Node import must perform no IO.
tsconfig copies the current frontend compilerOptions and includes only app.js;
no npm dependency or package manifest. HTML uses static inline CSS and app.js.

The seven-file write set is a bounded first-launch exception: two entry/backend
files, three small assets/config and two tests. Do not expand it into a generic
server/security framework. Reuse unchanged shared ChildServer/raw HTTP helpers
by #[path]; exact startup prefix per contract avoids a new launcher. Test HTTP
using the real binary and port0, not a substitute executable.

## Exact exclusive write set

- `apps/cli/src/main.rs`
- `apps/cli/src/business_demo.rs`
- `apps/cli/business-demo/index.html`
- `apps/cli/business-demo/app.js`
- `apps/cli/business-demo/tsconfig.json`
- `apps/cli/business-demo/app.test.mjs`
- `apps/cli/tests/business_demo_http.rs`

No implicit registration writes elsewhere. Existing listed files may be changed
only for this increment; new feature modules include their own unit tests.

## Required tests and browser evidence

Rust unit names (under business_demo::tests):
`business_demo_company_save_is_atomic`, `business_demo_amount_and_text_limits`,
`business_demo_epoch_revision_and_reset`, `business_demo_detached_get_is_read_only`.
Use table-driven zero/overflow/invalid currency/text/control and stale cases.

HTTP integration names: `business_demo_startup_and_routes`,
`business_demo_origin_body_and_method_rejections`,
`business_demo_save_conflict_and_reset`, `business_demo_assets_are_local`.
Check positive GET with no Content-Length, correct POST Origin/custom header,
missing/duplicate Host/Origin, unknown/query target, wrong methods/media,
missing/chunked/oversized body, malformed/unknown/duplicate JSON fields and
unchanged state on denial. Library-generated errors need observed assertions,
not presumed S1 transport semantics. Add named HTTP test
`business_demo_library_prehandler_responses`: unknown
`Expect: unsupported-demo` yields417/empty before the handler; an otherwise
valid `Expect: 100-continue` request reaches the handler and yields application
400 JSON/security headers without interim100. A malformed header line
(`GET / HTTP/1.1\r\nBadHeader\r\n\r\n`) yields library400/empty;
an abruptly closed partial request may have no HTTP response. Assert full
view unchanged after each, using a subsequent valid GET. Do not require
application JSON/security headers on pre-handler library responses or add a
parser to synthesize them. Use request Connection:close in raw fixtures
so shared read_response obtains complete tiny_http responses.

Node names: `request envelope binds epoch and revision`,
`money formatting does not use floating point totals`,
`module import performs no browser or network work`.
At least4 named unit,5 named HTTP and3 named Node tests execute.

Reviewer browser check: open business-demo at both viewport sizes; edit company/service name, scope
and price2500→3500, Save, navigate Today/company, reload, reset. Test invalid
price and stopped-server reload failure; verify literal text rendering and
labels. Capture one successful form screenshot and one rejected field state.

## Execution and acceptance

One card, one serial writer; preserve other changes. Implement only the linked
contract sections and this exact write set. No self-edits to cards, contract,
backlog, S1/legacy source, dependencies, gates or controller records. No commits,
merge, push, external messages or provider calls by the worker. Controller
records base/card/input blobs and handles reviewed local integration.

Worker: Luna medium, attempt budget30min, maxLunaFailures2; reviewer/fallback:
Astra high. Two substantive failures follow [protocol](../protocol.md) without
resetting attempt history. No speculative framework or automatic next card.

Run the exact per-card command block in [contract §12](../business-demo-contract.md#12-test-ownership-and-commands).
Enumerate and run this card's tests; zero/skipped mandatory tests are failures.
The reviewer runs the applicable full/S1 integration gates once for the wave,
checks actual browser behavior for this increment and binds results to the
candidate. No timers/usability metrics without an already-committed script.

Sole extra write: `docs/handoff/codex/reports/S2-01A1.md`. Raw evidence only in
`.harness/s2-01a1/`. Report base/revision/attempt/candidate binding, actual
visible behavior, diff stat, every new/untracked file, test names/counts and
commands with skipped/failure distinctions, reuse/new tools, deviations and
remaining work. Return candidate for independent review; do not self-accept.

Tool inventory: existing workspace::parse_amount_cents (only pure Workspace
call), serde/JSON, tiny_http, rand/hex; existing ChildServer::start_command,
stop/captured_output and raw_request/request HTTP helpers; FakeRoot, RustLexer,
S1 source/manifest/isolation tests, test_changed. Store/ops/render_document/
WorkflowRunner/authority/model/vault are not simulation APIs: their persistent,
UUID/time or authority effects remain outside this command. No Python benchmark
helpers exist here. Additional domain/form code is the only declared new tool;
no duplicate parser/capture/writer/hash/state framework.

> 复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
