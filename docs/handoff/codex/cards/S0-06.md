# S0-06 — 脚手架独立验收与下一张产品卡

**Revision 2 · Spec Frozen · 执行角色：reviewer。**运行状态唯一见 [backlog](../../../backlog.md)。分派前必须由 controller 填入当前 base_commit、卡 blob 与唯一报告路径；未填不得施工。

## 目标与前置

确认开发机制确实可运行，再将下一张 Playground 任务送入架构冻结/施工。

S0-05 已验收。

## 精确写入集合

- 无产品或工具写入；仅 controller 追加该卡报告/验收与队列记录。

本卡只追加 controller 指定的里程碑验收报告与队列记录，不产生 worker report 或实现 attempt；其余规范和工具均只读。

## 接口与做法

验收 [protocol](../protocol.md) 和 [contracts v2](../contracts.md)；此卡不允许补实现。发现问题返回具名修复卡，修复后重新独立审阅。过去的文档评审不能代替此次运行验收。

1. 读取真实 candidate diff、所有 S0 测试、模型分派记录和失败/恢复试演。
2. 由 controller/gate runner 在干净 candidate worktree 运行适用集成门；reviewer 核对实际日志与准确 candidate，并将报告作为独立记录提交。
3. 检查没有把 read-only TOML、模型自报身份或 JSON 日志当作不可伪造的安全证明。
4. 核对旧 lane 并发情况。只有实际 S0 门通过才能允许 Luna 担任日常 controller；下一张正常卡为 S1-00，有具名修复依赖时先修复。
5. 审核 [自举交接](../models-and-goals.md#bootstrap-controller)：强模型交接报告绑定验收证据、实际分派路径、CLI、当前集成提交与停止/释放记录。Luna 只能在新卡重新取得锁，不能改 owner 接管旧 attempt。

## 验收

以下新增测试/检查为 **planned**，不得在报告里预填通过。先确认测试已存在且实际执行，零测试/跳过不通过。

- S0-02 至 S0-05 全部命名测试实际存在且执行。
- 直接成功/第一次失败/第二次失败/恢复/旧结果失效路径有真实运行结果。
- 每次真实模型分派的请求参数和可观察回报有记录。
- 真实 fallback 调用与模拟失败标注清楚；作者和 reviewer 的上下文确实不同。
- 新 controller 能据交接记录找到下一卡、完整历史与检查入口，无需依赖旧会话记忆。

~~~bash
node --test --test-reporter=tap scripts/codex-control/state.test.mjs scripts/codex-control/git.test.mjs scripts/codex-control/checks.test.mjs scripts/codex-control/lock.test.mjs scripts/codex-control/cli.test.mjs scripts/codex-control/rehearsal.test.mjs
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo fmt --all --check
./scripts/check-file-size.sh
./scripts/test_changed.sh
git diff --check
~~~

不修改代码、不在验收中偷偷放宽预算或放弃失败路径；不能以本卡完成宣称 Playground 已实现。

## 交接

本卡严格按 [protocol §9.4.1](../protocol.md) 作为 reviewer-only 里程碑验收：列出既有候选与检查/审阅引用，不生成新的 worker attempt 或虚构 candidate。报告结论交 controller 核对并更新 backlog；发现实现问题生成具名修复卡，不在验收中直接改代码。

工具清单唯一维护于 [protocol §10.2](../protocol.md)，controller 分派简报时展开实际相关条目并补充已落地 S0 接口。新开发工具的必要性已在 [contracts §1](../contracts.md) 申报；除此以外的新工具必须先申报。

> 复用以上，禁止重新实现同类工具；需要新工具先在简报回复中申报。
