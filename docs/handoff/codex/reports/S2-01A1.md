# S2-01A1 — Astra fallback candidate

**Outcome:** stabilized candidate submitted for independent review; no acceptance or commit claimed. This is the frozen company/service increment, not full S2 or full MVP.

## Binding and history

- Dispatch/current base: `70913c43d0073c08ee6dd072ac68b68b4a2e1a46`.
- Card revision 3, blob `6b82038ddb684578141464479747a8b772025964`; scoped contract blob `1a8047774ddef8ea4fe7827fe93336d306486d4c`.
- Actual actor: Astra high, fallback after two independently confirmed Luna failures. No third Luna attempt and no reset of failure history.
- Both failed candidates/reviews remain under `.harness/s2-01a1/attempt1/` and `attempt2/`, including their `root-candidate/` manifests. All fallback evidence is separate under `.harness/s2-01a1/fallback/`.

| Source path | Candidate Git blob |
| --- | --- |
| apps/cli/src/main.rs | `343a92ae4a491a54d2542dd8ed9dbf5f37252528` |
| apps/cli/src/business_demo.rs | `3d5714b204388821e3e1c6d1af24c7569e7d876c` |
| apps/cli/business-demo/index.html | `48f0d97f74a5361926a62ced306a0619dc9c1ae7` |
| apps/cli/business-demo/app.js | `548590bd8f3ba4aa14a89ef85212d7f7173ebef0` |
| apps/cli/business-demo/tsconfig.json | `5eb80f1a98fc8f0a1854c450ac489d6a5d473ae5` |
| apps/cli/business-demo/app.test.mjs | `2fe8551ddf2695b8b4fae348b53b2ef786784c47` |
| apps/cli/tests/business_demo_http.rs | `f40f096ad5115dda1f3cb3b780398ddeb64fc394` |

The final eight-file manifest additionally binds this report. The six source paths other than main.rs, and this report, are untracked; ordinary `git diff` alone omits them. Tracked diff: main.rs, 8 insertions. New source/test/config lines: 1,485 (657 backend, 241 JS, 82 Node tests, 45 HTML, 1 config, 459 HTTP tests). No other tracked paths changed.

## Behavior and repair

The separate `business-demo` command serves an editable synthetic company/service form and Today summary. Save commits a complete validated clone, reset confirms inline, errors retain invalid field entries, conflicts refresh once, and transport/unknown-result failures remove actionable state until Reload succeeds. DOM construction uses textContent/value rather than HTML interpolation. One busy flag covers navigation, forms and reset. SGD prices display grouped integer-derived totals.

State validation now covers short/long UTF-8 text and controls, positive bounded money, checked revisions, detached views, bounded serialization and a reset epoch distinct within four draws. Strict route/method/Host/Origin/custom-header/media/framing/JSON checks retain unchanged state on denial. Security headers/CSP apply to application responses; actual tiny_http pre-handler empty errors/disconnect behavior remains unchanged. No owner authorization, model, persistence, effects or security-runtime behavior is fabricated.

## Tests and actual commands

Fresh fallback regressions were written before fixes. `red-unit.log` records two passes and two failures (short-name newline accepted; mutation beyond revision limit accepted). `red-node.log` records three passes and one failure (render returned HTML rather than void). These are newly authored probes, not reconstructed historical RED evidence. Initial implementation compile/type failures remain in `green-unit-1.log`, `http-1.log` and `tsc-1.log`; fixed errors were the static header-name lifetime and malformed JSDoc typedef.

All following final commands exited 0:

```sh
cargo test -p sovereign-cli --locked business_demo -- --list
cargo test -p sovereign-cli --locked business_demo
cargo test -p sovereign-cli --locked --test business_demo_http
node --experimental-default-type=module --test --test-reporter=tap apps/cli/business-demo/app.test.mjs
npx -y -p typescript@5.5.4 tsc -p apps/cli/business-demo/tsconfig.json
cargo clippy -p sovereign-cli --all-targets --locked -- -D warnings
cargo fmt --all
TEST_CHANGED_LOG=.harness/s2-01a1/fallback/full-gate.log ./scripts/test_changed.sh
git diff --check
```

Final evidence: `test-names.log`, `tests-3.log`, `http-3.log`, `node-3.log`, `tsc-3.log`, `clippy-3.log`, `full-gate.log` and `full-gate-summary.log`. The scoped gate returned ALL GREEN for gate-self-test, file-size, fmt, workspace clippy, workspace tests and frontend tsc. Business-demo tsc was also run explicitly above. No source edits followed this green gate.

- Six Rust units execute: the four required `business_demo_company_save_is_atomic`, `business_demo_amount_and_text_limits`, `business_demo_epoch_revision_and_reset`, `business_demo_detached_get_is_read_only`, plus `business_demo_reset_collision_and_view_bound_are_atomic` and `business_demo_serialization_failure_is_not_success`.
- Five required A1 HTTP tests execute: `business_demo_startup_and_routes`, `business_demo_origin_body_and_method_rejections`, `business_demo_save_conflict_and_reset`, `business_demo_assets_are_local`, `business_demo_library_prehandler_responses`. The complete HTTP binary reports eight passes including three unchanged shared capture regression tests. Matrices check duplicate/missing headers, framing/media, malformed/unknown/duplicate JSON, stale/reset behavior and full unchanged views after rejection. Library417/empty, malformed-header400/empty, supported-Expect application400 and partial EOF are observed separately.
- Six Node tests execute, zero skipped: the three required envelope/money/import tests plus frozen void render, one in-flight/exact fetch policy, and conflict refresh/no POST replay/failure state clearing.
- The name-filtered cargo command reports zero selected tests in playground_isolation; this is not counted as S1 isolation evidence. Full workspace tests ran through the gate; independent release isolation/browser acceptance remains outstanding.

## Browser evidence and bounds

Own debug binary ran at port55498, initially PID10322 (port0), later PID13939 after restart on the same port. Ego task space18 exercised literal HTML-like text (zero img nodes), price2500→3500, invalid0 retention/aria-invalid, reset cancel/confirm, 375px width without horizontal overflow, conflict recovery after a competing reset, and stopped-server failure followed by working Reload. DOM/form submission was used for the final conflict and stopped-server probes. `browser-checks.log` preserves observations and probe mistakes; an initial competing-reset request used the wrong custom header/envelope and got403 before its corrected200 request. Immediate snapshots sometimes preceded completion and are not treated as successful saves. The later conflict message and restored seed are observed; no claim of a complete captured browser network trace is made.

Ego/CDP screenshot attempts timed out. A separate CUA Chrome visual check captured the actual debug page successfully: `cua-today.png`, `cua-company.png`, `cua-rejected-price.png`. CUA independently opened the company form and submitted price0 before its rejection screenshot. These visual captures are separate from the Ego interaction evidence. They do not constitute release-browser acceptance. Both owned preview processes, Ego task space18 (done:true), and the CUA tab were closed; root preview7788 was untouched.

## Reuse, scope and remaining work

Reused pure `workspace::parse_amount_cents`, serde/JSON, tiny_http, rand/hex and unchanged `ChildServer::start_command`, stop/captured_output, raw_request/request/parser helpers. Existing FakeRoot/RustLexer/source/manifest/isolation checks and test_changed remain unchanged. No new dependency, launcher, capture/parser/writer/hash framework, or generic tool was introduced; new code is bounded domain/form implementation and its tests. No card, contract, S1, legacy UI, gate or controller-record edit.

Independent reviewer must decide this exact candidate and perform the requested release/browser acceptance. No known card-specific implementation dependency remains. Later company/customer/discovery and other S2 increments, real AI/legal integration, and full MVP acceptance remain separate required work; later S2 design row13 is outside this card.
