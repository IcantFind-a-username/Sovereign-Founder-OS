# 文档脚手架验收记录 · 2026-09-09

**结论：accepted-with-current-maturity。**只验收文档集的可用性，不是 S0-06 运行验收。

- 文档候选：08a51539da4dab84fbc575b6ac4826c1d0c992c0。
- 产品事实核对基线：933af77。
- 唯一执行入口：[README](../README.md)。
- 范围：产品研究、规范寻址、Codex 执行协议、工具契约、首批冻结卡、后续设计阻塞、backlog 引用。
- 变更统计：候选提交包含 18 个文档文件，1657 行新增、3 行删除；无产品源码、运行时配置或依赖变化。

## 已解决的问题

| 问题 | 最终处理 |
| --- | --- |
| 单一长文容易与 backlog/后续卡重复维护 | 产品判断、执行规则、精确接口、任务定义分别有唯一位置，统一从 README 寻址。 |
| Task 1 的精确 token/零依赖检查阻止正常扩展 | S1-00 明确阶段快照与永久不变量；S1-G01/G02 及后续窄迁移先行，普通功能 worker 不改 gate。 |
| 浏览器交互验收早于 server | 顺序调整为 server → browser actions → CLI → 真实进程隔离。 |
| Candidate 恢复可能跳过独立 reviewer 检查 | 恢复 Candidate 后仍需 review_start；InReview 恢复保持原绑定。 |
| 证据验证接口没有实际作者/reviewer 上下文 | validateEvents 产生 CandidateBinding；verifyEvidence 读取 events 并核对 Review.actor。 |
| Reviewer-only 卡被套入实现 attempt | protocol 单独定义里程碑汇总验收，S0-06 不制造 worker/candidate。 |
| 有库接口却无确定 controller 入口 | S0-05A 冻结 CLI 请求/输出/退出码，S0-05 试演依赖该入口。 |
| Read-only reviewer 无法写构建产物 | controller/gate runner 在隔离候选 worktree 运行固定门，reviewer 核对确切 HEAD、日志和代码。 |

## 实际验证

- 独立只读文档复核：协议修正后接受；未据此宣称任何未来运行器已实现。
- 本地 Markdown 文件链接与代码围栏检查通过；新 lane 的队列 ID 无重复。
- git diff --check：通过。
- ./scripts/test_changed.sh：ALL GREEN；执行 gate-self-test、file-size、fmt。
- 此候选只修改文档，scoped gate 没有 Cargo test scope；未把它描述为重新通过全量产品测试。
- 使用当前仓库 owner Git 身份；没有修改产品、安装角色配置或创建开发 Goal/定时任务。
- 工具复用：复核既有 source_boundary、manifest_boundary、lexer/fixtures 及信任链入口；未新增或复制运行时工具。

## 交接

S0-00 的文档契约由本次文档任务交付。下一张实现卡为 S0-01，之后按依赖推进 S0-02、S0-03、S0-04、S0-05A、S0-05，再执行 S0-06 独立运行验收。S1 功能卡仍须等待其架构与边界迁移前置。

实际队列状态只维护在 [backlog](../../../backlog.md)；本记录是历史证据，不是第二份待办或当前状态表。
