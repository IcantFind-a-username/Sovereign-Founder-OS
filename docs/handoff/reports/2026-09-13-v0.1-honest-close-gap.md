# v0.1 honest-close gap review

**Status:** Planning / gap only (2026-09-13). No implementation.
**Scouted:** `main` @ `84a5389` (#116) **and** open PRs **#117–#140**.
None of #117–#140 are merged. RFC 0006 is unchanged on `main` and on #121.
**Supersedes as status:** #117 close-plan snapshot (that plan is still the
ticket map; this note is the post-Composer stack review).

Owner-approved honest close (binding, #121): **v0.1 = fixture/mechanism
proof + honest product labels.** Product 1C0 admission → **v0.2**. RFC 0006
G2 stays conjunctive.

---

## 1. Exit criteria 1–5 (honest-close)

| # | Honest-close reading | Verdict now | After merging the named stack |
| --- | --- | --- | --- |
| **1** | Clean install of the local founder loop without source knowledge | **Partial** on `main` | Still **Partial** until Wave E |
| **2** | Experimental outbox labelled as app-signed; no product-1C0 claim; evidence for *built* fixture pieces only | **Partial** on `main` (labels already in ROADMAP current-state; product still mints `owner_approval_key`; fixture crate exists, E2E not wired) | **Met** after #121 + Wave A. Wave D is **not** required. |
| **3** | Program 1A engine evidence, not product enrollment | **Partial** on `main` (crate + SQLCipher pin + FFI; no wrappers/schema/importer/qualify) | **Partial → near-met** after Wave C stack; residuals below keep it from a clean **Met** |
| **4** | Bind supported workspace values **and** document exclusions | **Partial** on `main` (inventory + chain; no value-auth; ROADMAP prose exclusions) | Still **Partial**. #123 *pins* the unbound/plaintext boundary — do **not** “fix” binding in v0.1. |
| **5** | One tagged, signed/checksummed Developer Preview, no production claim | **Missing** (`git tag -l` empty; `release.yml` is a disabled stub) | Still **Missing** until Wave E + owner tag policy |

### Evidence pointers

**Exit 1 — Partial (`main`)**

- Loop exists: MVP design + HTTP E2E `the_business_flow_runs_end_to_end_over_http`.
- Install is still `git clone` + `cargo run` (`README.md`). No preview artifact.
- `--root` still open (backlog ~1502 and duplicate ~1757). Desktop bundle is a
  manual script. Five-consultant protocol open (`docs/backlog.md:1611`).

**Exit 2 — Partial (`main`) / Met (draft honesty + #121)**

- `main` ROADMAP still has the **old** product-1C0 exit 2 (`ROADMAP.md:201-205`).
  That conflicts with RFC 0006 G2. **#121 (ready, CI green)** rewrites it.
- Honesty pins (draft): #118 (CI green, not draft), #119, #120, #122, #123
  (CI green), #136, #137.
- Fixture on `main`: `crates/owner` feature-gated, `publish = false`, default
  build empty. Broker leftover is v1-shaped (`NotImplemented` / HMAC path).
  Exact-grant E2E not wired. That is **allowed** under honest-close; it is
  **not** a v0.1 close blocker.

**Exit 3 — Partial (`main`) / Partial-near-met (Wave C drafts)**

- `main`: `crates/vault-v2-engine/` (`publish = false`), runtime 4.14.0 pin,
  FFI/process/secret present. Named Task 1 tests
  `connection_profile_matches_every_required_pragma` /
  `raw_dbk_never_reaches_sql_text_logs_or_environment` land in **#132**.
- Draft stack: #132 → #134 → #135 → #138 → #139 → #140 (#133 is a sibling of
  #134; #140 cherry-picks qualify-script content).
- Still not a clean Met after that stack (implementer residuals):
  - **v01-16 leftover:** `successful_read_only_reopen_preserves_hash_len_and_no_journal` (needs a checked-in fixture DB).
  - **v01-19 leftovers (#135):** getrandom **per-backend compile-reject** matrix; `SystemEntropy` not on all production fills; `Prepared*` production enrollment/rotation still stubs; native keyring `BadEncoding`/`BadDataFormat` canary; authorizer “remove one layer” negatives only partial.
  - **v01-21 leftovers (#139):** importer happy path + role-key block exists; plan Task 4 **failpoint / cooperating-writer / CAS** matrix is not the full plan list (plan `:1123-1147`, `:1233-1240`).
- CLI still depends only on `sovereign-vault`. No product enrollment. Good.

**Exit 4 — Partial (`main` + #123)**

- `verify_export` is inventory + device/chain (`types.rs`).
- #123 adds `export_boundary_plaintext_and_workspace_not_authenticated`.
- Literal ROADMAP text still says “bind supported workspace values”. Honest
  close treats that as **labelled Partial**, not a Composer “authenticate
  workspace” ticket.

**Exit 5 — Missing**

- Provenance snapshot forbids a `v0.1` product tag
  (`docs/superpowers/specs/2026-08-27-public-provenance-release-design.md`).
- Tag family must be `developer-preview-YYYY.MM.DD.N`, not `v0.1`.

---

## 2. Must-merge ordered stack

Almost every ticket independently edits `docs/backlog.md`. Expect conflict
noise. Rebase/queue; do not squash across waves blindly.

### Track 0 — governance (do first)

| Order | PR | Draft | CI (observed) | Notes |
| --- | --- | --- | --- | --- |
| 1 | **#121** G1 ROADMAP honest-close | no | **green** | Owner-approved. Merge now. |
| — | #117 close plan | yes | n/a | Historical ticket map. Merge after or skip; do not treat as current status. |

### Track A — honesty (parallel after #121)

| Order | PR | Draft | CI | Deps |
| --- | --- | --- | --- | --- |
| A1 | **#118** v01-01 gateway docs | no | **green** | none |
| A2 | #119 v01-02 CLI locality | yes | (assume green / recheck) | none |
| A3 | #120 v01-03 T10 freeze | yes | | none |
| A4 | #122 v01-04 v1 blob pins | yes | | **#120** (branch already includes it) |
| A5 | #123 v01-05 export pin | yes | **green** | none |
| A6 | #136 v01-06 leftover copy | yes | | **#121 first** — drop the 463→571 ROADMAP hunk |
| A7 | #137 v01-07 stale admissions row | yes | | none |

### Track B — Experimental-outbox integrity (no 1C0)

| Order | PR | Draft | CI | Deps / blockers |
| --- | --- | --- | --- | --- |
| B1 | #124 v01-08 journal recover | yes | **test red: rustfmt** | fmt-only; cheap fix |
| B2 | #125 v01-09 checkpoint-gap pin | no | test green; **fixture red** `dropping_the_lock_releases_it` (v1 broker lock — likely flake / unrelated) | hold until fixture understood |
| B3 | #126 v01-10 `ledger.head` | yes | **green** | none |
| B4 | #127 v01-11 refuse reverted ledger | yes | no checks yet | **#126** (stacked) |
| B5 | #128 v01-12 SIGKILL soak | yes | **green** | none |
| B6 | #129 v01-13 two-process one effect | yes | **green** | includes #128 commits; bases on `main` |
| B7 | #130 v01-14 adversarial invariants | yes | **green** | exports `workspace` as lib — rebase after B1/B4/B6 |
| B8 | #131 v01-15 compile worker on send | yes | **green** | none; rebase if workspace lock from #129 lands first |

Merge B sequentially once they touch `apps/cli/src/workspace/`.

### Track C — Program 1A (linear)

```
#132 (fmt red) ─┬─ #133 qualify          ─┐
                └─ #134 AST ─ #135 ─ #138 ─ #139 ─ #140 (cherry-picks #133)
```

| Order | PR | Blocker |
| --- | --- | --- |
| C1 | **#132** v01-16 named tests | **test red: rustfmt** — blocks the stack |
| C2 | #134 v01-17 AST/UI fixtures | after #132 |
| C3 | #135 v01-19 dual wrappers | after #134; residuals listed in §1 |
| C4 | #138 v01-20 closed schema | after #135 |
| C5 | #139 v01-21 importer | after #138; failpoint matrix residual |
| C6 | #133 v01-18 qualify script | after #132 **or skip** if merging #140 |
| C7 | #140 v01-22 platform + non-activation | after #139; needs macOS/Windows runners to mean anything |

---

## 3. Remaining work to honestly claim Developer Preview close

### Merge-only / docs

- Merge **#121** then Track A (mark drafts ready when CI is green).
- Merge Track B after fmt/#125 fixture disposition.
- Merge Track C after #132 fmt.
- Optionally merge #117 as the frozen ticket map, or leave it.
- One-line ROADMAP current-state: test count 571 (already in #121/#136).
- Do **not** retag RFC 0006. Do **not** claim exit 4 “binds workspace values”.

### Small Composer tickets (after merge, one `/iterate` each)

1. **fmt-only:** #124, #132 (`cargo fmt` and push). Highest leverage.
2. **v01-19 residuals** (split, do not heroics): getrandom compile-reject
   matrix; `SystemEntropy` production fills; keyring canary; authorizer
   teardown negatives. `Prepared*` production paths can wait — they are
   Task 4 handoff stubs, not product enrollment.
3. **v01-21 failpoint/race slice** (plan Task 4 Step 1 durable CAS /
   cooperating-writer). Keep `publish = false`.
4. **`--root`** (collapse duplicate backlog ~1502/~1757). Unblocks Wave E
   testers without a tag.
5. **v01-16 fixture DB** for the read-only reopen named test (optional for
   honesty; required for a strict exit-3 Met).

### Grok / governance

- **v01-G2:** RFC 0002 amendment `low-risk-effectful` / `write_rfc5322`
  (`rg` on `main` is empty). Seven-day discussion + maintainer acceptance.
  **Blocks all Wave D.** An implementation PR must not accept its own RFC.
- **Preview tagging policy:** `developer-preview-*` only; never `v0.1`
  until all five exits are actually Met under this reading. Owner + CI
  secrets, not unattended Composer (`v01-33`).
- **Exit 4 wording:** either leave as honest Partial, or a later owner
  ROADMAP diff that matches the #123 pin. Not Composer.

### Human-only

- **v01-31** five-consultant usability (`docs/backlog.md:1611`). Not a
  kernel gate; cannot be substituted by Wave C.
- Founder install copy for the preview artifact (`v01-34`, after a tag).

---

## 4. Next three coordinator actions

1. **Merge #121 now.** Mark Wave A ready and merge #118, #119, #120→#122,
   #123, #136 (rebase ROADMAP), #137. This is what makes exit 2 **Met**
   under honest-close. **Do not start Wave D.**
2. **Unblock rustfmt (#124, #132)** — one-line Composer each — then merge
   Track B green PRs in the table order. That is residual hardening of the
   *Experimental* outbox, not 1C0.
3. **After #132 is green, merge Track C** for exit-3 evidence. Then choose
   **E over D**: start `--root` + tagging-policy discussion (Wave E). Queue
   v01-19/v01-21 residuals as optional hardening, not as a Wave D substitute.
   Hold Wave D until G2 is accepted.

**Wave D vs E vs residual (one line):** D is forbidden until G2. E is the
founder-visible close. Residuals are how you turn exit 3 from Partial into
Met; they are not required to talk about a labelled Developer Preview.

---

## 5. Non-goals still forbidden

- Product 1C0 / WebAuthn on `/api/workspace/decide` / removing
  `kernel_exec` `owner_approval_key`.
- Continuing superseded v1 broker (`BrokerReady`, HMAC IPC, hidden child,
  second listener).
- RFC 0004 product boundary, Amber/Green removal, `Local Only` zero-egress.
- Confined local-model worker / digest binding / ambient deny.
- Programs 1B0/1B1, 1C1, 1D `PendingV2`/`ActiveV2`, product Vault
  enrollment or migration.
- Network email, OAuth broker, encrypted backup, restore, whole-device
  rollback detection.
- Owned Mesh / multi-device E2EE.
- A `v0.1` **product** git tag.
- Production / enterprise assurance language.
- Wave D fixture code before G2 acceptance.
- “Fixing” export to authenticate workspace as a v0.1 close trick.
- Five-consultant protocol invented by a model (`v01-31` is observational).
