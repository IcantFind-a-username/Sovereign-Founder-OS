# Disabled SemVer product publication gate. Do not add tag triggers or
# release publishers here. Developer Preview uses
# .github/workflows/developer-preview.yml per docs/release/preview-tag-policy.md.

name: Product Release Gate

on:
  workflow_dispatch:

permissions: {}

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Report disabled publication gate
        run: |
          echo "Automatic product publication is disabled."
          echo "The documented product-release gates are not yet implemented."
